# Sprint Reset Brief — 2026-06-14

## Mode
PROJECT RESET. All previous worker agents removed. Leader remains.
Builder / Guardian / Ops just spawned (model: MiniMax-M3, YOLO).

Sprint 4 + Sprint 5 are being completed TOGETHER. Do not relitigate old
Sprint 4/5 decisions, worktree anomalies, or the dispatch.txt history in
`docs/sprint5/`. The repo state on `main` is the starting point.

## Existing Code — READ IT FIRST
The repo already contains substantial partial work from prior sprints:

Backend (Go, in `src/`):
- `internal/agentfactory/` — agent factory + tests
- `internal/aion/` — Aion runtime (mock, process, runtime, tests)
- `internal/dispatch/` — dispatcher + queue + tests
- `internal/events/` — bus + state + tests
- `internal/handler/` — agent, assignment, capability, deliverable, execution,
  project, task, review, code, webhook, auth, user, deployment, types
- `internal/service/` — same surface, business logic
- `internal/model/` — domain models incl. agent, capability, assignment,
  execution, deliverable, worker, agent_type
- `internal/store/postgres/` — pg-backed stores incl. agent, assignment,
  assignment_event, deliverable, execution
- `internal/middleware/`, `internal/validation/`
- Migrations `005..026` cover agents, agent_runs, executions, deliverables,
  agent_registry, agent_capabilities, task_required_capabilities, assignments,
  assignment_events, deliverable_versioning, deliverable_versions, executions
  (aion_instance_id), agents.runtime

Frontend (Next.js, in `frontend/src/`):
- `app/agents/` CRUD + status + capabilities + activity pages
- `app/tasks/` + `app/assignments/`
- `app/deliverables/` + versions
- `app/dashboard/`, kanban board, agent activity, lifecycle timeline
- `lib/api.ts`, `lib/stores.ts`, `lib/realtime.ts`, `lib/types.ts`,
  `lib/queryKeys.ts`
- `providers/AuthProvider.tsx`, `RealtimeProvider.tsx`, `NotificationProvider.tsx`
- `components/agents/`, `components/deliverables/`, `components/kanban/`,
  `components/activity/`

Treat all of the above as the floor. Task = make the deliverable SHIPPED:
tests pass, build passes, Guardian sign-off, Ops secret-scan clean, pushed
to main.

## Quality Gate (NO COMMIT until ALL true)
- [ ] Tests pass (`go test ./...` and `npm test`)
- [ ] Build passes (`go build ./...` and `npm run build`)
- [ ] Guardian review completed (signature in PR / dispatch log)
- [ ] Security validation: no secrets, no API keys, no tokens, no credentials,
      no AI conversation data, no temp execution logs
- [ ] `.gitignore` covers any new artifact class
- [ ] Commit message format: `feat(task-A001): <imperative summary>`
      (or `fix(...)`, `chore(...)`, `docs(...)`, `test(...)`)
- [ ] Pushed to main by Ops after Builder's local commit

## Sprint Objective (11 deliverables)
1. Agent Registry             → A-001 Builder
2. Capability System           → A-002 Builder
3. Task Assignment Engine      → A-003 Builder
4. Execution Engine            → B-001 Builder
5. Agent Communication Layer   → B-002 Builder
6. Deliverable Storage         → B-003 Builder (Ops assist)
7. Monitoring Dashboard        → C-001 Builder (Ops assist)
8. Recovery System             → C-002 Builder (Guardian assist)
9. Testing Framework           → D-001 Guardian (Builder assist)
10. Security Review            → D-002 Guardian (Leader assist)
11. Workflow E2E Validation    → D-003 Guardian (Ops assist)

Plus operational:
- E-001 Infrastructure Validation  → Ops
- E-002 GitHub Protection           → Ops
- E-003 Continuous Commit Policy    → Ops (applies to EVERY task)

## Working Rules
- Every agent: one task at a time, mark `in_progress` when starting,
  `completed` when committed+push OK, log evidence in dispatch file.
- No idle. If your queue is empty, scan the board for unassigned work in
  your support area, OR assist the owner.
- No infinite retry loops. If blocked >2 attempts: log blocker to
  `docs/reset/blockers.md`, create mitigation task, execute alt work.
- No secrets. No temp logs in commits. No `dispatch.txt`-style noise in
  the main tree — that pattern is for the worktree branches only.
- Communication: ping the target agent by slot_id. Keep the chat short.
  Detail in files under `docs/reset/`.
- Worktree pattern (optional, for parallel work): use `git worktree add
  ../wt-<task-id> -b feat/<task-id>` and merge via Ops to main.
- Leader will assist implementation when orchestration is light.

## First Wave
- Builder: A-001 audit + close gaps
- Guardian: D-001 test framework + audit existing test coverage matrix
- Ops: E-001 infra validation + E-002 secret scan + clean working tree
  of the Sprint 5 closeout worktree dirs (`..devops01-wt-*`,
  `dev01-task-427-wt/`, `dispatch.txt`, `out*.txt`, `tmp*.ps1`,
  `tmp_commit_msg.txt`)

## Board
14 tasks created (A-001..A-003, B-001..B-003, C-001..C-002, D-001..D-003,
E-001..E-003). E-003 is the standing continuous-commit policy.

Begin.
