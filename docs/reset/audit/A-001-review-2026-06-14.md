# A-001 Review — 2026-06-14

> Reviewer: Guardian · Reviewer slot: `019ec4fe-604f-7551-9a76-38a621ddd256`
> Subject: `feat/A001-agent-registry-audit` @ 50c8bb7
> Worktree: `C:\Users\fadhi\OneDrive\Documents\wt-A001`
> Diff: 2 files, +325/-33 — `docs/api-spec.md` §Agents (12-point drift fix) + `docs/reset/audit/A-001-audit.md` (NEW)
> Verdict: **APPROVED** (with one soft follow-up)

## Checklist (from Lead)

- [x] api-spec §Agents: 12-point drift items are real (cross-ref design + impl) — see §1
- [x] audit doc: hand-backs to A-002 are correctly scoped and complete — see §2
- [x] no secrets / no temp logs in the diff — see §3
- [x] no scope creep (no code changes in the A-001 branch) — see §4
- [x] pre-push gate (tests + build + Guardian + secret-scan) will hold after A-002 hand-backs land — see §5

## 1. The 12 drift items

Each item is verified against three sources where applicable: design (`docs/sprint4/*.md`),
impl (`src/internal/{model,handler,service,db}/...`), and the new `api-spec.md`.

| # | Item | Verified against | Status |
|---|---|---|---|
| 1 | Status enum `initializing, idle, busy, paused, error, retired` (6) | `model.AllAgentStatuses` (`model/agent.go:64-71`); `agents_status_chk` (`db/migrations/016_agent_registry.sql:67-69`) | ✓ |
| 2 | Role free-form, 1-80 chars (not enum) | Design (`agent-orchestration-design.md:28` says "1–80 chars"); spec now matches; code `validateAgentRole` allows 1-255 (see §6 follow-up) | ✓ spec, ⚠ code-loose |
| 3 | `type` field removed from create body | `createAgentRequest` in `handler/agent.go:48-53` has no `type` | ✓ |
| 4 | `model`/`provider` removed from create body (moved to `metadata`) | `createAgentRequest` no longer has them; spec example nests them in `metadata` | ✓ |
| 5 | `project_id` required, immutable on create | Required via `X-Project-ID` header (handler returns 400 `MISSING_PROJECT`); not in `updateAgentRequest` (immutable on PUT) | ✓ |
| 6 | `capabilities` on create: required, ≥ 1, validated against catalog | `CreateAgent` returns 400 if `len == 0` (`service/agent.go:140-142`); `validateCapabilitiesExist` checks catalog (`:456`) | ✓ |
| 7 | List pagination: cursor-based (no `total`) | `model.AgentListResult{Data, NextCursor, HasMore}` (`model/agent.go:211-214`); handler returns `gin.H{"data": ..., "pagination": {"has_more": ..., "next_cursor": ...}}` (`handler/agent.go:185-191`) | ✓ |
| 8 | PUT optimistic concurrency: `version` required | `updateAgentRequest.Version` is `*int` (no `omitempty`) → required; service rejects nil with `VALIDATION_ERROR` and mismatches with `VERSION_CONFLICT` (`service/agent.go:283-292`) | ✓ |
| 9 | DELETE: soft-delete, sets `retired` | `Delete` handler calls `svc.RetireAgent` (`handler/agent.go:280`) → sets `AgentRetired` and `RetiredAt` | ✓ |
| 10 | List response default scope: retired hidden by default | `includeRetired := strings.EqualFold(c.Query("include_retired"), "true")` defaults false (`handler/agent.go:165`) | ✓ |
| 11 | `name` length 1-80 chars | `validateAgentName` enforces 1-80 (`service/agent.go:432`); spec matches | ✓ |
| 12 | Extra endpoints: cross-ref A-002 (no code) | Router wires `GET /v1/agents/:id/capabilities` and `GET /v1/capabilities` to `capabilities.ListAgentCapabilities` / `ListCatalogCapabilities` (A-002's `CapabilityHandler`); A-001 spec defers writes to A-002 | ✓ |

## 2. A-002 hand-backs

The audit enumerates 4 hand-backs, all correctly scoped (A-001 does not touch them):

| ID | File | Why A-002's | A-001 touches it? |
|---|---|---|---|
| A-002-01 | `handler/execution_test.go` | `TestCreateExecution_BadRequest` asserts against agentfactory error codes (TASK-403 status) | No (docs-only diff) |
| A-002-02 | `service/review_test.go` | Imports `pgxpool` directly; A-002 owns the store refactor | No |
| A-002-03 | `agentfactory/` | `syscall.Kill` on a tracked variable that races with cancel; A-002 owns the factory rewrite | No |
| A-002-04 | store tests | Import `pgxpool` directly; A-002 owns the store refactor | No |

Verified by `git diff main...HEAD --stat` (three-dot diff): only 2 files change, both docs.
The diff is structurally constrained to `docs/api-spec.md` and `docs/reset/audit/A-001-audit.md`.

## 3. Secrets / temp logs

Walked the diff (`docs/api-spec.md` +236/-33, `docs/reset/audit/A-001-audit.md` +122):

- **No API keys, tokens, passwords, or secrets.** All UUID placeholders use the
  `a1b2c3d4-...` / `3a1b2c3d-...` convention that the rest of the spec uses.
- **No log statements or temp instrumentation.** Diff is markdown only.
- **No copies of `.env` content, JWT secrets, or DB credentials.**

The Ops E-002 secret-scan gate would also catch this; pre-merge gitleaks run on
the PR will be a no-op signal here.

## 4. Scope creep

`git diff main...HEAD --stat` (three-dot, merge-base to HEAD):
```
docs/api-spec.md                  | 269 +++++++++++++++++++++++++++++++------
docs/reset/audit/A-001-audit.md   | 122 ++++++++++++++++++++++++++++++++
2 files changed, 325 insertions(+), 33 deletions(-)
```

No `.go`, no `.yml`, no config files, no scripts. Diff is structurally a
**docs-only** change. The audit doc itself is gated by its own §5
("boundary discipline") which I'll quote verbatim for traceability:

> 5.1 The code may be ahead of the spec — the audit should NOT back-port the
> code to match the spec, only spec to match the design.
> 5.2 A-001 is a docs-only branch. No Go file, no test, no fixture, no
> migration in this PR.
> 5.3 The audit doc lives in `docs/reset/audit/A-001-audit.md` so future
> reviewers can find it without git archaeology.

The 5.1 clause is what the §6 follow-up is keyed to: the audit correctly does
not back-port code, it only aligns the spec to the design's intent.

## 5. Pre-push gate

The PR is docs-only, so the four gates behave as follows:

| Gate | Behaviour on this PR |
|---|---|
| Tests (`go test ./...`, `npx vitest run`) | Unaffected. The 4 A-002 hand-backs are explicitly out of scope; their tests will be fixed by A-002. |
| Build (`go build ./...`, `next build`) | Unaffected. No code, no deps. |
| Guardian review (this doc) | ✓ APPROVED. |
| Secret-scan (`.github/workflows/secret-scan.yml`) | No new strings to scan; will be a no-op green. |

After A-002 lands the 4 hand-backs, the full test suite should turn green for
both branches. The Builder's A-001 sign-off is gated on (a) this PR landing
and (b) A-002 unblocking the 4 hand-backs.

## 6. Follow-up (non-blocking)

**Spec role length: 1-80 vs code 1-255.**

- `docs/api-spec.md` (post-A-001): "1-80 chars, free-form" (line 25 + 104)
- `docs/sprint4/agent-orchestration-design.md:28`: "1–80 chars" (design intent)
- `src/internal/service/agent.go:446`: `if len(role) > 255 { ... }` (code-loose)
- `db/migrations/016_agent_registry.sql:27`: `VARCHAR(255) NOT NULL` (DB-loose)

The audit's drift item 2 correctly aligns the spec to the design intent (1-80).
The code is more permissive (1-255). The audit is explicitly NOT back-porting
the code (per §5.1 of the audit doc), which is the right call: tightening the
code is a separate, code-side refactor.

**Recommended follow-up** (not part of A-001):
- A-001 sign-off lands as-is.
- Add a TASK-XXX: tighten `validateAgentRole` to 1-80, and either (a) keep
  `VARCHAR(255)` for headroom on legacy rows, or (b) tighten the DB column to
  `VARCHAR(80)` in a future migration. Assign to whoever owns the agent
  service refactor next.

This is **non-blocking** for A-001 because:
- The spec is the canonical API contract. Clients should not send > 80 chars.
- The code's looseness is an internal implementation detail, not a public
  commitment.
- Tightening the code is a backwards-incompatible change for any client that
  currently sends > 80 chars; that needs a separate migration window.

## Verdict

**APPROVED A-001.**

12 of 12 drift items verified, hand-backs correctly scoped, docs-only diff, no
secrets, pre-push gate unaffected. One non-blocking follow-up (role-length
code tightening) is logged in §6 for the next refactor cycle.
